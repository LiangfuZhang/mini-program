<view class="container">
    <view class="title">{{fence.title}}</view>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="index">
            <s-cell class="cell" cell="{{item}}" x="{{x}}" y="{{index}}"></s-cell>
        </block>
    </view>
    <view class="hr"></view>
</view>